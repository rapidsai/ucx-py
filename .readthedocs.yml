version: 2

build:
  image: latest
  apt_packages:
    - libnuma1

python:
  version: 3.9
  install:
    - method: pip
      path: .

conda:
  environment: conda/environments/builddocs.yml

sphinx:
  configuration: docs/source/conf.py
